#!/bin/bash

export PATH=/var/vcap/jobs/terraform-apply/bin:$PATH

# terraform.lifecycle.purge-on-delete-deployment
#FIXME: test expected deployment. If deployment delete, should terraform clean everything
#. ./terrafom-plan-apply