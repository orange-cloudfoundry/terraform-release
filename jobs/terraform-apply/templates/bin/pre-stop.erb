#!/bin/bash

export PATH=/var/vcap/jobs/terraform-apply/bin:$PATH

# terraform.lifecycle.purge-on-delete-deployment

if [[ "${BOSH_DEPLOYMENT_NEXT_STATE}" == "delete" ]] ; then
    set -e
    terraform destroy
fi