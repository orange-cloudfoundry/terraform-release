#!/bin/bash

export PATH=/var/vcap/jobs/terraform-apply/bin:$PATH

<% if_p('terraform.lifecycle.pre-stop') do |value| %>
<% if value %>
export PLAN_ONLY=true
terraform-plan-apply
<% end %>


<% end %>
